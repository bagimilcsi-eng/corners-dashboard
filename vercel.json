{
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter @workspace/stats-dashboard run build",
  "outputDirectory": "artifacts/stats-dashboard/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
